package decimal.apigateway.enums;

public enum Headers {

    apiName, requestid, orgid, appid, logorgid, logappid, username, password, deviceid, loginid, servicename, nounce,clientsecret,txnkey,hash, sourceappid, destinationappid, sourceorgid, destinationorgid, auth_scheme

}
