package decimal.apigateway.enums;

public enum HeadersV1 {

    apiName, requestid, orgid, appid, logorgid, logappid, username, password, deviceid, loginid, servicename, nounce,clientsecret,txnkey,hash
}
